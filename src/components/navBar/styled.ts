import { makeStyles } from '@mui/styles';

export const useNavBarStyles = makeStyles({
  navBar__icon: {
    transform: 'scale(1.7)',
  },
  toolbar: {
    maxWidth: '1440px',
    width: '100vw',
    margin: '0 auto',
  },
});
