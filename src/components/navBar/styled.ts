import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  root: {
    height: '8vh',
    padding: '2rem',
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
  navBar__icon: {
    transform: 'scale(2)',
  },
});
